{
  "name": "whaswapp-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm --filter desktop tauri dev",
    "start": "pnpm --filter desktop start",
    "clean": "pnpm -r clean",
    "build:release": "pnpm --filter desktop build:release"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
